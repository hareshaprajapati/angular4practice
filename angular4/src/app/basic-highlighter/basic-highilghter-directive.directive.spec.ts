import { BasicHighilghterDirectiveDirective } from './basic-highilghter-directive.directive';

describe('BasicHighilghterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHighilghterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
